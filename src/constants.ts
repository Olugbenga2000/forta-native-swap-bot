
const MINIMUM_SWAP_COUNT = 2;
const ERC20_TRANSFER_EVENT = "event Transfer(address indexed from, address indexed to, uint256 value)";
const LOW_NONCE_THRESHOLD = 150;
const MIN_ETH_THRESHOLD = 30;
const MAX_MINUTES_BETWEEN_SWAPS = 30;

export {
    MINIMUM_SWAP_COUNT,
    ERC20_TRANSFER_EVENT,
    LOW_NONCE_THRESHOLD,
    MIN_ETH_THRESHOLD,
    MAX_MINUTES_BETWEEN_SWAPS
}

